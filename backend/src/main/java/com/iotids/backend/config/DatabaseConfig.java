package com.iotids.backend.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class DatabaseConfig {
    // Basic placeholder for database-specific configuration like custom Dialects, EntityManagers etc.
    // Most config is handled automatically via application.properties in Spring Boot.
}
